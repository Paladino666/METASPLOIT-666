#!/bin/bash

clear

setterm -foreground green -store

pkg install wget -y

clear

setterm -foreground green -store

clear

pkg install ruby -y

clear

setterm -foreground green -store

clear

setterm -foreground green -store

clear

pkg install curl -y

clear

setterm -foreground green -store

clear

figlet -f mono12 "METASPLOIT"

termux-open-url https://www.youtube.com/channel>

setterm -foreground green -store

clear

wget https://raw.githubusercontent.com/Hax4us/M>

clear

setterm -foreground green -store

clear

curl -O https://raw.githubusercontent.com/th3un>

clear

setterm -foreground green -store

clear

chmod 777 metasploit.sh

setterm -foreground green -store

clear

./metasploit.sh

setterm -foreground red -store

clear
